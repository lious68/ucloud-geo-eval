# UI 精修 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 精修 UCloud GEO 前端 UI：统一配色/字号/间距/圆角为 CSS 变量，37 处 emoji 全量替换为 Element Plus 图标，删除脚手架死代码，重点精修 Dashboard/Login/侧边栏。

**Architecture:** 纯样式 + 图标替换，不动 props/store/router/接口。在 `App.vue` 全局样式中建立 `:root` CSS 变量，各页面引用变量并替换硬编码值；emoji 替换为已全局注册的 `@element-plus/icons-vue` 图标组件。

**Tech Stack:** Vue 3 (SFC), Element Plus 2.14, @element-plus/icons-vue 2.3, ECharts 6, Vite 8。无单元测试框架，验证手段为 `npm run build` 无报错。

## Global Constraints

- 不修改任何 `<script setup>` 中的业务逻辑、API 调用、props、store、router。
- 图标组件已在 `frontend/src/main.js` 全局注册（`for ... app.component(key, component)`），模板内直接 `<el-icon><Xxx/></el-icon>` 使用，无需 import。
- emoji 替换图标映射（已验证存在于 `@element-plus/icons-vue`）：🎯→Aim, 📊→DataAnalysis, 📈→TrendCharts, 🚀→Promotion, 🕐→Clock, 🔗→Link, 📝→Document, ⚙️→Setting, 📡→Aim, 👍→Medal, 💛→Sunny, 🏆→Trophy, ⓘ→InfoFilled, 📋→View, 🔄→Refresh, ➕→Plus, 📥→Upload, ⬇→Download, 🌐→Monitor, ⚠️→WarningFilled, 📎→Paperclip, 🔑→Key, ⚖️→Histogram, 👥→User。
- 配色变量值（精确）：`--color-primary:#409eff`, `--color-text:#1a1a2e`, `--color-text-sec:#666`, `--color-border:#ebeef5`, `--color-page-bg:#f0f2f5`, `--color-card:#fff`, `--radius:10px`, `--radius-lg:16px`, `--fs-page-title:22px`, `--fs-section-title:16px`, `--space:16px`。
- 所有命令在 `C:/Users/Administrator/ucloud-geo-eval/frontend` 下执行。
- 每个 Task 结束运行 `npm run build` 验证无报错。

---

## File Structure

修改文件：
- `frontend/src/App.vue` — 删 `style.css` import 依赖不存在但清理侧边栏 emoji；新增全局 `:root` 变量；侧边栏 logo/指示器 emoji→图标
- `frontend/src/views/Dashboard.vue` — 指标卡/表格/下钻 emoji→图标，字号统一
- `frontend/src/views/Login.vue` — 🎯→Aim
- `frontend/src/views/History.vue` — 🕐/📈/📊 emoji→图标，标题统一 22px
- `frontend/src/views/CitationSources.vue` — 🔗/📎 emoji→图标，标题 28px→22px
- `frontend/src/views/Questions.vue` — 📝 emoji→图标
- `frontend/src/views/Settings.vue` — ⚙️/🚀/🔑/⚖️/👥 emoji→图标
- `frontend/src/views/TaskList.vue` — 🚀/🔄/➕/📥/⬇/⚠️ emoji→图标
- `frontend/src/views/TaskDetail.vue` — 标题 emoji 清理
- `frontend/src/components/BatchDownloadDialog.vue` — 无 emoji，仅确认

删除文件：
- `frontend/src/style.css`
- `frontend/src/components/HelloWorld.vue`
- `frontend/src/assets/hero.png`
- `frontend/src/assets/vite.svg`
- `frontend/src/assets/vue.svg`

---

## Task 1: 删除脚手架死代码

**Files:**
- Delete: `frontend/src/style.css`
- Delete: `frontend/src/components/HelloWorld.vue`
- Delete: `frontend/src/assets/hero.png`
- Delete: `frontend/src/assets/vite.svg`
- Delete: `frontend/src/assets/vue.svg`

**Interfaces:** 无 — 这些文件均未被任何 import 引用（已通过 grep 验证：`style.css` NOT imported anywhere；`HelloWorld` NOT referenced；assets 仅在自身 svg 内引用异色）。

- [ ] **Step 1: 删除 5 个死文件**

Run:
```bash
cd C:/Users/Administrator/ucloud-geo-eval/frontend
rm -f src/style.css src/components/HelloWorld.vue src/assets/hero.png src/assets/vite.svg src/assets/vue.svg
```

- [ ] **Step 2: 确认无残留引用**

Run:
```bash
cd C:/Users/Administrator/ucloud-geo-eval/frontend
grep -rn "style.css\|HelloWorld\|hero.png\|vite.svg\|vue.svg" src/ index.html || echo "NO REFERENCES"
```
Expected: `NO REFERENCES`

- [ ] **Step 3: 构建验证**

Run: `npm run build`
Expected: 构建成功，无报错。

- [ ] **Step 4: Commit**

```bash
cd C:/Users/Administrator/ucloud-geo-eval
git add -A frontend
git commit -m "chore: 删除 Vite 脚手架死代码 (style.css/HelloWorld/assets)"
```

---

## Task 2: App.vue 全局变量 + 侧边栏精修

**Files:**
- Modify: `frontend/src/App.vue`

**Interfaces:**
- Produces: 全局 CSS 变量 `--color-primary/--color-text/--color-text-sec/--color-border/--color-page-bg/--color-card/--radius/--radius-lg/--fs-page-title/--fs-section-title/--space`，供后续所有 Task 的页面引用。

- [ ] **Step 1: 替换 `<style>` 全局块，新增 `:root` 变量并统一 body/侧边栏配色**

将 `App.vue` 中第一个 `<style>` 块（约 117-134 行）整体替换为：

```css
<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
:root {
  --color-primary: #409eff;
  --color-primary-soft: rgba(64, 158, 255, 0.1);
  --color-text: #1a1a2e;
  --color-text-sec: #666;
  --color-border: #ebeef5;
  --color-page-bg: #f0f2f5;
  --color-card: #fff;
  --radius: 10px;
  --radius-lg: 16px;
  --fs-page-title: 22px;
  --fs-section-title: 16px;
  --space: 16px;
}
body { font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", sans-serif; background: var(--color-page-bg); color: var(--color-text); }
.app-container { height: 100vh; }
.sidebar { background: linear-gradient(180deg, #1a1a2e 0%, #16213e 100%); overflow-y: auto; display: flex; flex-direction: column; }
.sidebar .logo { padding: 20px; text-align: center; border-bottom: 1px solid rgba(255,255,255,0.1); display: flex; align-items: center; justify-content: center; gap: 8px; }
.sidebar .logo .el-icon { font-size: 24px; color: var(--color-primary); }
.sidebar .logo-text { color: #fff; font-size: 18px; font-weight: 700; }
.sidebar-menu { border-right: none !important; flex: 1; }
.sidebar .el-menu { background: transparent; }
.sidebar .el-menu-item { color: rgba(255,255,255,0.7); border-radius: 8px; margin: 2px 8px; }
.sidebar .el-menu-item:hover { color: #fff; background: rgba(255,255,255,0.1); }
.sidebar .el-menu-item.is-active { color: #fff; background: rgba(15,52,96,0.6); border-right: 3px solid var(--color-primary); }
.sidebar-footer { padding: 12px; border-top: 1px solid rgba(255,255,255,0.1); }
.user-info { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 0 4px; }
.user-name { color: rgba(255,255,255,0.9); font-size: 14px; font-weight: 600; line-height: 24px; }
.main-content { padding: var(--space); overflow-y: auto; background: var(--color-page-bg); }
</style>
```

- [ ] **Step 2: 模板 logo 🎯→Aim 图标**

将 App.vue 模板中：
```html
      <div class="logo">
        <span class="logo-icon">🎯</span>
        <span class="logo-text">UCloud GEO</span>
      </div>
```
替换为：
```html
      <div class="logo">
        <el-icon class="logo-icon"><Aim /></el-icon>
        <span class="logo-text">UCloud GEO</span>
      </div>
```

- [ ] **Step 3: 评测运行指示器 emoji→图标**

将运行指示器块中：
```html
        <div class="eval-indicator-header">
          <span class="eval-indicator-icon">🚀</span>
          <span class="eval-indicator-title">评测运行中</span>
          <el-tag v-if="evalStore.evalMode === 'webchat'" size="small" type="info">🌐</el-tag>
        </div>
```
替换为：
```html
        <div class="eval-indicator-header">
          <el-icon class="eval-indicator-icon"><Promotion /></el-icon>
          <span class="eval-indicator-title">评测运行中</span>
          <el-tag v-if="evalStore.evalMode === 'webchat'" size="small" type="info" class="mode-tag"><el-icon><Monitor /></el-icon></el-tag>
        </div>
```

- [ ] **Step 4: 心跳 ⚠️→WarningFilled**

将：
```html
          <span v-else-if="evalStore.heartbeatStalled" class="hb-text warn">⚠️ 可能挂了</span>
```
替换为：
```html
          <span v-else-if="evalStore.heartbeatStalled" class="hb-text warn"><el-icon><WarningFilled /></el-icon> 可能挂了</span>
```

- [ ] **Step 5: 退出登录按钮 emoji 已是 SwitchButton 图标，清理 .logo-icon 旧样式**

将 scoped style 中 `.eval-indicator-icon { font-size: 16px; }` 替换为：
```css
.eval-indicator-icon { font-size: 16px; color: var(--color-primary); }
.mode-tag .el-icon { font-size: 12px; vertical-align: middle; }
```

- [ ] **Step 6: 构建验证**

Run: `npm run build`
Expected: 构建成功。

- [ ] **Step 7: Commit**

```bash
cd C:/Users/Administrator/ucloud-geo-eval
git add -A frontend
git commit -m "style: App.vue 全局 CSS 变量 + 侧边栏 emoji→图标"
```

---

## Task 3: Login.vue 精修

**Files:**
- Modify: `frontend/src/views/Login.vue`

**Interfaces:** 无（叶子页面）。

- [ ] **Step 1: 标题 🎯→Aim**

将模板：
```html
      <div class="login-header">
        <span class="logo-icon">🎯</span>
        <h2>UCloud GEO 评估系统</h2>
        <p v-if="isFirstLogin" style="color:#e6a23c;font-size:13px;margin-top:8px">首次使用，请设置管理密码</p>
      </div>
```
替换为：
```html
      <div class="login-header">
        <el-icon class="logo-icon"><Aim /></el-icon>
        <h2>UCloud GEO 评估系统</h2>
        <p v-if="isFirstLogin" class="first-login-tip">首次使用，请设置管理密码</p>
      </div>
```

- [ ] **Step 2: 统一配色，logo-icon 改图标样式**

将 `<style scoped>` 整块替换为：
```css
.login-page { display: flex; justify-content: center; align-items: center; min-height: 100vh; background: linear-gradient(135deg, #1a1a2e 0%, #0f3460 100%); }
.login-card { background: var(--color-card); border-radius: var(--radius-lg); padding: 40px; width: 400px; box-shadow: 0 20px 60px rgba(0,0,0,0.3); }
.login-header { text-align: center; margin-bottom: 30px; }
.login-header .logo-icon { font-size: 48px; color: var(--color-primary); }
.login-header h2 { color: var(--color-text); margin-top: 12px; }
.first-login-tip { color: #e6a23c; font-size: 13px; margin-top: 8px; }
```

- [ ] **Step 3: 构建验证**

Run: `npm run build`
Expected: 构建成功。

- [ ] **Step 4: Commit**

```bash
cd C:/Users/Administrator/ucloud-geo-eval
git add -A frontend
git commit -m "style: Login.vue emoji→图标 + 配色统一"
```

---

## Task 4: Dashboard.vue 精修

**Files:**
- Modify: `frontend/src/views/Dashboard.vue`

**Interfaces:** 无（叶子页面，但本文件最大，分多步）。

- [ ] **Step 1: 页标题 📊→DataAnalysis**

将：
```html
    <h2 class="page-title">📊 GEO 评估仪表盘</h2>
```
替换为：
```html
    <h2 class="page-title"><el-icon><DataAnalysis /></el-icon> GEO 评估仪表盘</h2>
```

- [ ] **Step 2: 指标定义中的 emoji 改为占位字符串（图标在卡片渲染时用）**

将 `metricDefinitions` 数组中 4 个 icon 字段：
- `icon: '📡'` → `icon: 'Aim'`
- `icon: '🔗'` → `icon: 'Link'`
- `icon: '👍'` → `icon: 'Medal'`
- `icon: '💛'` → `icon: 'Sunny'`

即把：
```js
  {
    key: 'coverage_rate', label: '提及率', icon: '📡',
```
改为：
```js
  {
    key: 'coverage_rate', label: '提及率', icon: 'Aim',
```
（其余 3 个同理改为 Link / Medal / Sunny）

- [ ] **Step 3: 空状态指标卡图标改动态组件**

将模板中：
```html
                <span class="intro-icon">{{ m.icon }}</span>
```
替换为：
```html
                <el-icon class="intro-icon"><component :is="m.icon" /></el-icon>
```

- [ ] **Step 4: 核心指标卡图标改动态组件**

将：
```html
                <span class="metric-icon">{{ m.icon }}</span>
```
替换为：
```html
                <el-icon class="metric-icon"><component :is="m.icon" /></el-icon>
```

- [ ] **Step 5: GEO 综合得分 🏆→Trophy**

将：
```html
                <span class="metric-icon">🏆</span>
```
替换为：
```html
                <el-icon class="metric-icon"><Trophy /></el-icon>
```

- [ ] **Step 6: 公式触发器 ⓘ→InfoFilled（卡片处）**

将两处（空状态卡 + 核心指标卡 + GEO 卡，共 3 处）：
```html
                  <span class="formula-trigger">ⓘ</span>
```
全部替换为：
```html
                  <el-icon class="formula-trigger"><InfoFilled /></el-icon>
```

- [ ] **Step 7: 表头公式触发器 ⓘ→InfoFilled**

将 channelDetails 表格中所有（4 处）：
```html
                  <span class="col-formula-trigger">ⓘ</span>
```
全部替换为：
```html
                  <el-icon class="col-formula-trigger"><InfoFilled /></el-icon>
```

- [ ] **Step 8: 明细查看 📋→View**

将：
```html
                <el-button type="primary" link size="small" @click="openDrilldown(row.model_key)">
                  📋 查看
                </el-button>
```
替换为：
```html
                <el-button type="primary" link size="small" @click="openDrilldown(row.model_key)">
                  <el-icon><View /></el-icon> 查看
                </el-button>
```

- [ ] **Step 9: 下钻抽屉空状态/加载无 emoji，跳过。统一样式变量**

将 `<style scoped>` 顶部三行：
```css
.page-title { font-size: 22px; margin-bottom: 20px; color: #1a1a2e; }
.section-title { font-size: 16px; font-weight: 600; color: #1a1a2e; margin-bottom: 14px; padding-left: 2px; }
```
替换为：
```css
.page-title { font-size: var(--fs-page-title); margin-bottom: 20px; color: var(--color-text); display: flex; align-items: center; gap: 8px; }
.section-title { font-size: var(--fs-section-title); font-weight: 600; color: var(--color-text); margin-bottom: 14px; padding-left: 2px; }
```

- [ ] **Step 10: metric-icon / intro-icon 适配图标尺寸**

将样式：
```css
.metric-icon { font-size: 18px; }
```
替换为：
```css
.metric-icon { font-size: 18px; color: var(--color-primary); }
```
将：
```css
.intro-icon { font-size: 20px; }
```
替换为：
```css
.intro-icon { font-size: 20px; color: var(--color-primary); }
```
将 `.metric-value { ... color: #1a1a2e; ... }` 中 `color: #1a1a2e` 改为 `color: var(--color-text)`。
将 `.formula-tooltip` 内 `color: #1a1a2e` 两处改为 `color: var(--color-text)`。
将 `.channel-name { font-weight: 600; color: #1a1a2e; }` 改为 `color: var(--color-text)`。
将 `.collapse-title { font-weight: 600; font-size: 14px; color: #1a1a2e; }` 改为 `color: var(--color-text)`。

- [ ] **Step 11: 构建验证**

Run: `npm run build`
Expected: 构建成功。

- [ ] **Step 12: Commit**

```bash
cd C:/Users/Administrator/ucloud-geo-eval
git add -A frontend
git commit -m "style: Dashboard emoji→图标 + 配色变量统一"
```

---

## Task 5: History.vue 精修

**Files:**
- Modify: `frontend/src/views/History.vue`

- [ ] **Step 1: 页标题 🕐→Clock + 副标题 📈→TrendCharts**

将：
```html
    <h2 class="page-title">🕐 历史评测情况</h2>
```
替换为：
```html
    <h2 class="page-title"><el-icon><Clock /></el-icon> 历史评测情况</h2>
```

将：
```html
        <h3 class="section-title">📈 评测趋势对比</h3>
```
替换为：
```html
        <h3 class="section-title"><el-icon><TrendCharts /></el-icon> 评测趋势对比</h3>
```

- [ ] **Step 2: WebChat 🌐→Monitor + 查看 📊→DataAnalysis**

将：
```html
              <el-tag v-if="row.mode === 'webchat'" type="info" size="small">🌐 WebChat</el-tag>
```
替换为：
```html
              <el-tag v-if="row.mode === 'webchat'" type="info" size="small"><el-icon><Monitor /></el-icon> WebChat</el-tag>
```

将：
```html
              <el-button size="small" type="primary" @click="viewResult(row.id)" :disabled="row.status!=='completed'">
                📊 查看
              </el-button>
```
替换为：
```html
              <el-button size="small" type="primary" @click="viewResult(row.id)" :disabled="row.status!=='completed'">
                <el-icon><DataAnalysis /></el-icon> 查看
              </el-button>
```

- [ ] **Step 3: 空状态按钮文案清理**

将：
```html
        <el-button type="primary" @click="$router.push('/evaluation')">执行第一次评测</el-button>
```
保留不变（无 emoji）。

- [ ] **Step 4: 样式变量统一**

将 `<style scoped>` 中：
```css
.page-title { font-size: 22px; margin-bottom: 20px; color: #1a1a2e; }
.section-title { font-size: 16px; font-weight: 600; color: #1a1a2e; margin-bottom: 14px; padding-left: 2px; }
```
替换为：
```css
.page-title { font-size: var(--fs-page-title); margin-bottom: 20px; color: var(--color-text); display: flex; align-items: center; gap: 8px; }
.section-title { font-size: var(--fs-section-title); font-weight: 600; color: var(--color-text); margin-bottom: 14px; padding-left: 2px; display: flex; align-items: center; gap: 8px; }
```

- [ ] **Step 5: 构建验证**

Run: `npm run build`
Expected: 构建成功。

- [ ] **Step 6: Commit**

```bash
cd C:/Users/Administrator/ucloud-geo-eval
git add -A frontend
git commit -m "style: History emoji→图标 + 配色变量统一"
```

---

## Task 6: CitationSources.vue 精修

**Files:**
- Modify: `frontend/src/views/CitationSources.vue`

- [ ] **Step 1: 页标题 🔗→Link + 标题字号 28→22**

将：
```html
    <h2 class="page-title">🔗 引用源情况</h2>
```
替换为：
```html
    <h2 class="page-title"><el-icon><Link /></el-icon> 引用源情况</h2>
```

- [ ] **Step 2: 下钻弹窗 📎→Paperclip**

将：
```html
        <el-dialog v-model="drilldownVisible" :title="`📎 ${drilldownSource} 引用详情`" width="820px" top="6vh">
```
替换为：
```html
        <el-dialog v-model="drilldownVisible" :title="drilldownTitle" width="820px" top="6vh">
```

- [ ] **Step 3: 增加 drilldownTitle 计算属性**

在 `<script setup>` 中 `const drilldownLoading = ref(false)` 之后新增：
```js
import { computed } from 'vue'
```
（若已 import computed 则跳过此行）并将该 import 行合并到顶部已有的 `import { ref, computed, onMounted, nextTick } from 'vue'`（确认顶部已是此行，无需改动）。

然后在 `drilldownLoading` 定义后新增：
```js
const drilldownTitle = computed(() => `${drilldownSource.value} 引用详情`)
```

- [ ] **Step 4: 样式变量统一 + 标题降为 22px**

将 `<style scoped>` 中：
```css
.page-title { font-size: 28px; margin-bottom: 6px; color: #0f172a; font-weight: 800; }
.page-subtitle { color: #64748b; margin-bottom: 22px; }
```
替换为：
```css
.page-title { font-size: var(--fs-page-title); margin-bottom: 6px; color: var(--color-text); font-weight: 700; display: flex; align-items: center; gap: 8px; }
.page-subtitle { color: var(--color-text-sec); margin-bottom: 22px; }
```

将 `.stat-value { font-size: 30px; font-weight: 800; color: #0f172a; }` 改为 `color: var(--color-text);`。
将 `.stat-label { color: #64748b; margin-bottom: 10px; }` 改为 `color: var(--color-text-sec);`。
将 `.filter-label { color: #64748b; ... }` 改为 `color: var(--color-text-sec);`。
将 `.loading-state { text-align: center; padding: 80px 0; color: #64748b; }` 改为 `color: var(--color-text-sec);`。
将 `.platform-row { ... color: #334155; }` 改为 `color: var(--color-text);`。

- [ ] **Step 5: 构建验证**

Run: `npm run build`
Expected: 构建成功。

- [ ] **Step 6: Commit**

```bash
cd C:/Users/Administrator/ucloud-geo-eval
git add -A frontend
git commit -m "style: CitationSources emoji→图标 + 标题字号统一"
```

---

## Task 7: Questions.vue 精修

**Files:**
- Modify: `frontend/src/views/Questions.vue`

- [ ] **Step 1: 页标题 📝→Document**

将：
```html
    <h2 class="page-title">📝 问题管理</h2>
```
替换为：
```html
    <h2 class="page-title"><el-icon><Document /></el-icon> 问题管理</h2>
```

- [ ] **Step 2: 新增按钮 ➕→Plus（已是图标，确认）**

将：
```html
        <el-button v-if="isAdmin()" type="primary" @click="openAddDialog"><el-icon><Plus /></el-icon> 新增问题</el-button>
```
保留不变（已用 Plus 图标）。

- [ ] **Step 3: 样式变量统一**

将 `<style scoped>`：
```css
.page-title { font-size: 22px; margin-bottom: 20px; color: #1a1a2e; }
```
替换为：
```css
.page-title { font-size: var(--fs-page-title); margin-bottom: 20px; color: var(--color-text); display: flex; align-items: center; gap: 8px; }
```

- [ ] **Step 4: 构建验证**

Run: `npm run build`
Expected: 构建成功。

- [ ] **Step 5: Commit**

```bash
cd C:/Users/Administrator/ucloud-geo-eval
git add -A frontend
git commit -m "style: Questions emoji→图标 + 配色变量统一"
```

---

## Task 8: Settings.vue 精修

**Files:**
- Modify: `frontend/src/views/Settings.vue`

- [ ] **Step 1: 页标题 ⚙️→Setting**

将：
```html
    <h2 class="page-title">⚙️ 系统设置</h2>
```
替换为：
```html
    <h2 class="page-title"><el-icon><Setting /></el-icon> 系统设置</h2>
```

- [ ] **Step 2: 卡片标题 emoji→图标（4 处）**

将：
```html
      <template #header><strong>🚀 ModelVerse 中转平台（推荐）</strong></template>
```
替换为：
```html
      <template #header><strong><el-icon><Promotion /></el-icon> ModelVerse 中转平台（推荐）</strong></template>
```

将：
```html
        <strong>🔑 模型 API Key 配置</strong>
```
替换为：
```html
        <strong><el-icon><Key /></el-icon> 模型 API Key 配置</strong>
```

将：
```html
      <template #header><strong>⚖️ 评分权重配置</strong></template>
```
替换为：
```html
      <template #header><strong><el-icon><Histogram /></el-icon> 评分权重配置</strong></template>
```

将：
```html
          <strong>👥 用户管理</strong>
```
替换为：
```html
          <strong><el-icon><User /></el-icon> 用户管理</strong>
```

- [ ] **Step 3: 样式变量统一**

将 `<style scoped>`：
```css
.page-title { font-size: 22px; margin-bottom: 20px; color: #1a1a2e; }
```
替换为：
```css
.page-title { font-size: var(--fs-page-title); margin-bottom: 20px; color: var(--color-text); display: flex; align-items: center; gap: 8px; }
```

- [ ] **Step 4: 构建验证**

Run: `npm run build`
Expected: 构建成功。

- [ ] **Step 5: Commit**

```bash
cd C:/Users/Administrator/ucloud-geo-eval
git add -A frontend
git commit -m "style: Settings emoji→图标 + 配色变量统一"
```

---

## Task 9: TaskList.vue 精修

**Files:**
- Modify: `frontend/src/views/TaskList.vue`

- [ ] **Step 1: 页标题 🚀→Promotion**

将：
```html
    <h2 class="page-title">🚀 执行评测（任务 → 批次 → 问题）</h2>
```
替换为：
```html
    <h2 class="page-title"><el-icon><Promotion /></el-icon> 执行评测（任务 → 批次 → 问题）</h2>
```

- [ ] **Step 2: 工具栏按钮 emoji→图标**

将：
```html
          <el-button v-if="isAdmin()" type="warning" plain :loading="recalculating" @click="onRecalcAll">
            🔄 重算全部评分
          </el-button>
          <el-button v-if="isAdmin()" type="primary" @click="openWizard">
            <el-icon><Plus /></el-icon> 新建任务
          </el-button>
```
替换为：
```html
          <el-button v-if="isAdmin()" type="warning" plain :loading="recalculating" @click="onRecalcAll">
            <el-icon><Refresh /></el-icon> 重算全部评分
          </el-button>
          <el-button v-if="isAdmin()" type="primary" @click="openWizard">
            <el-icon><Plus /></el-icon> 新建任务
          </el-button>
```

- [ ] **Step 3: 批次行按钮 emoji→图标（3 处）**

将：
```html
                  <el-button v-if="isAdmin()" size="small" type="primary" plain @click="openBatch(row)">
                    ➕ 添加批次
                  </el-button>
```
替换为：
```html
                  <el-button v-if="isAdmin()" size="small" type="primary" plain @click="openBatch(row)">
                    <el-icon><Plus /></el-icon> 添加批次
                  </el-button>
```

将：
```html
                    <el-button v-if="isAdmin()" size="small" link type="success" @click="openImport(b)">
                      📥 导入
                    </el-button>
                    <el-button size="small" link type="primary" @click="downloadBatchConfig(b)">
                      ⬇ 配置
                    </el-button>
```
替换为：
```html
                    <el-button v-if="isAdmin()" size="small" link type="success" @click="openImport(b)">
                      <el-icon><Upload /></el-icon> 导入
                    </el-button>
                    <el-button size="small" link type="primary" @click="downloadBatchConfig(b)">
                      <el-icon><Download /></el-icon> 配置
                    </el-button>
```

- [ ] **Step 4: 任务行操作按钮 emoji→图标**

将：
```html
            <el-button v-if="row.coverage_rate > 0" size="small" type="primary" @click="viewResult(row)">
              📊 查看结果
            </el-button>
```
替换为：
```html
            <el-button v-if="row.coverage_rate > 0" size="small" type="primary" @click="viewResult(row)">
              <el-icon><DataAnalysis /></el-icon> 查看结果
            </el-button>
```

- [ ] **Step 5: 导入历史标题 📥→Upload + 提示 ⚠→WarningFilled**

将：
```html
                        <div class="import-logs-head">
                          📥 导入历史（{{ (batchImportLogsOf(b.batch_id) || []).length }} 次）
                        </div>
```
替换为：
```html
                        <div class="import-logs-head">
                          <el-icon><Upload /></el-icon> 导入历史（{{ (batchImportLogsOf(b.batch_id) || []).length }} 次）
                        </div>
```

将：
```html
                          <div v-if="r.error_message" class="result-error">⚠ {{ r.error_message }}</div>
```
替换为：
```html
                          <div v-if="r.error_message" class="result-error"><el-icon><WarningFilled /></el-icon> {{ r.error_message }}</div>
```

- [ ] **Step 6: 导入对话框拖拽提示无 emoji，跳过。导入对话框按钮无 emoji，跳过。样式变量统一**

将 `<style scoped>`：
```css
.page-title { font-size: 22px; margin-bottom: 20px; color: #1a1a2e; }
```
替换为：
```css
.page-title { font-size: var(--fs-page-title); margin-bottom: 20px; color: var(--color-text); display: flex; align-items: center; gap: 8px; }
```

将 `.result-error { color: #c0392b; font-size: 13px; }` 改为 `.result-error { color: #c0392b; font-size: 13px; display: flex; align-items: center; gap: 4px; }`。

- [ ] **Step 7: 构建验证**

Run: `npm run build`
Expected: 构建成功。

- [ ] **Step 8: Commit**

```bash
cd C:/Users/Administrator/ucloud-geo-eval
git add -A frontend
git commit -m "style: TaskList emoji→图标 + 配色变量统一"
```

---

## Task 10: TaskDetail.vue 清理 + 最终全量验证

**Files:**
- Modify: `frontend/src/views/TaskDetail.vue`

- [ ] **Step 1: 检查 TaskDetail 是否含 emoji**

Run:
```bash
cd C:/Users/Administrator/ucloud-geo-eval/frontend
grep -nE "🎯|📊|🕐|📝|⚙️|🚀|🔗|📡|👍|💛|🏆|🌐|📋|📈|🔄|➕|📥|⬇|ⓘ|⚠️|📎" src/views/TaskDetail.vue || echo "NO EMOJI"
```
Expected: `NO EMOJI`（TaskDetail 模板已确认无 emoji）。

- [ ] **Step 2: 全量残留 emoji 扫描**

Run:
```bash
cd C:/Users/Administrator/ucloud-geo-eval/frontend
grep -rnE "🎯|📊|🕐|📝|⚙️|🚀|🔗|📡|👍|💛|🏆|🌐|📋|📈|🔄|➕|📥|⬇|ⓘ|⚠️|📎|🔑|⚖️|👥" src/ || echo "NO EMOJI LEFT"
```
Expected: `NO EMOJI LEFT`

- [ ] **Step 3: 全量硬编码异色扫描（确认已迁移到变量）**

Run:
```bash
cd C:/Users/Administrator/ucloud-geo-eval/frontend
grep -rnE "#0f172a|#8900ff|#64748b" src/views/ src/App.vue || echo "NO STRAY COLORS"
```
Expected: `NO STRAY COLORS`（CitationSources 的 #64748b 已在 Task 6 替换为变量；若残留则补改）。

- [ ] **Step 4: 最终构建验证**

Run: `npm run build`
Expected: 构建成功，无 warning（除可能的 chunk size 提示）。

- [ ] **Step 5: Commit（若有改动）**

```bash
cd C:/Users/Administrator/ucloud-geo-eval
git add -A frontend
git diff --cached --quiet && echo "nothing to commit" || git commit -m "style: UI 精修收尾 + 全量验证"
```
